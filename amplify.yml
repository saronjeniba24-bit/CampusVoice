version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build needed for static HTML/JS"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
