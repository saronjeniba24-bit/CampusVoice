version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        build:
          commands:
            - echo "No build needed for static HTML/JS"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []
